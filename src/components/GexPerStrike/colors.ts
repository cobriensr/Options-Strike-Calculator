/**
 * Overlay accent colors used across GexPerStrike views.
 *
 * These complement the dark theme but are intentionally not pulled from
 * `theme` — they encode semantic meaning (charm vs vanna vs dex) that is
 * independent of the app's primary/accent palette.
 */

export const CHARM_POS = '#ffd740';
export const CHARM_NEG = '#ff6e40';
export const VANNA_POS = '#40c4ff';
export const VANNA_NEG = '#e040fb';
export const DEX_POS = '#69f0ae';
export const DEX_NEG = '#ff8a80';
