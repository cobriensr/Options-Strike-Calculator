# Dependencies
node_modules/

# Build output
dist/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Testing
coverage/

# Python service artifacts (sidecar/, uw-stream/, classifier/, ml/)
.venv/
.coverage
.pytest_cache/
__pycache__/
*.pyc

# Logs
*.log
npm-debug.log*

# Vitest
__snapshots__/

# Playwright
/test-results/
/blob-report/
/playwright/.cache/
/playwright-report/

# OS
.DS_Store
*.pem

# Claude — ignore everything by default, but un-ignore the three SKILL.md
# files that api/trace-live-analyze.ts loads at module init. They're project
# IP (analytical lens for charm/delta/gamma pressure) and must ship in the
# Vercel function bundle. Vercel's bundler skips gitignored files even when
# matched by includeFiles, so they have to be tracked.
.claude/*
!.claude/skills
.claude/skills/*
!.claude/skills/charm-pressure
!.claude/skills/delta-pressure
!.claude/skills/gamma
!.claude/skills/periscope
.claude/skills/charm-pressure/*
.claude/skills/delta-pressure/*
.claude/skills/gamma/*
.claude/skills/periscope/*
!.claude/skills/charm-pressure/SKILL.md
!.claude/skills/delta-pressure/SKILL.md
!.claude/skills/gamma/SKILL.md
!.claude/skills/periscope/SKILL.md
!.claude/skills/periscope/references
!.claude/skills/periscope/references/**
.vercel
.env*.local
.clone/

# Sonar
.scannerwork/

# Playwright
.playwright-mcp/

# Serena
.serena/

# ML
ml/.venv/
ml/*.csv
ml/data/
ml/trace/results/
ml/trace/images/
ml/.pytest_cache/
ml/.ruff_cache/
ml/htmlcov/
ml/.coverage

# Parquet data outputs (binary, often >20MB). Regenerable from the
# experiment scripts; the committed JSON summaries are the durable
# artifacts. Specific dirs that need a parquet tracked can git add -f.
**/*.parquet

# Python bytecode caches — single pattern covers all current + future subdirs
**/__pycache__/

# Experimental ML run outputs — regenerable from ml/scripts/.
# Findings + reports markdown docs are the durable summaries.
ml/experiments/sweeps/
ml/experiments/pac_a2/
ml/experiments/pac_replay/
ml/experiments/pac_classifier/
ml/experiments/ichimoku_classifier/
ml/experiments/sweep_run_*.log
ml/experiments/phase2_early_*.json

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Superpowers
.superpowers/

# ml-sweep Railway service
ml-sweep/.env*
ml-sweep/.venv/

# Capture-study scratch dirs — track code (.py / .ts / .mjs / .md) only;
# ignore data outputs (CSVs, screenshots, study artifacts, auth state).
scripts/charm-pressure-capture/.trace-storage.json
scripts/charm-pressure-capture/screenshots/
scripts/charm-pressure-capture/*.csv
scripts/charm-pressure-capture/*.csv.bak
scripts/charm-pressure-capture/findings/
scripts/charm-pressure-capture/plots/
scripts/charm-pressure-capture/scoring/
scripts/delta-pressure-capture/screenshots/
scripts/delta-pressure-capture/*.csv
scripts/delta-pressure-capture/*.csv.bak
scripts/gamma-capture/screenshots/
scripts/gamma-capture/*.csv
scripts/gamma-capture/*.csv.bak
scripts/eod-flow-analysis/output/backfill-buckets/

# Lottery Finder backtest (docs/tmp/options-flow-analysis/) — track scripts
# and load-bearing outputs (p14, p26-p31, macro_*) referenced by the spec at
# docs/superpowers/specs/lottery-finder-2026-05-02.md. Ignore exploratory
# intermediates from earlier phases (q*, p1-p9, p11-p22) so they don't
# pollute git on every pipeline re-run.
docs/tmp/options-flow-analysis/outputs/q*.csv
docs/tmp/options-flow-analysis/outputs/p1_*.csv
docs/tmp/options-flow-analysis/outputs/p2_*.csv
docs/tmp/options-flow-analysis/outputs/p3_*.csv
docs/tmp/options-flow-analysis/outputs/p4_*.csv
docs/tmp/options-flow-analysis/outputs/p5_*.csv
docs/tmp/options-flow-analysis/outputs/p6_*.csv
docs/tmp/options-flow-analysis/outputs/p6b_*.csv
docs/tmp/options-flow-analysis/outputs/p6c_*.csv
docs/tmp/options-flow-analysis/outputs/p6d_*.csv
docs/tmp/options-flow-analysis/outputs/p7_*.csv
docs/tmp/options-flow-analysis/outputs/p8_*.csv
docs/tmp/options-flow-analysis/outputs/p9_*.csv
docs/tmp/options-flow-analysis/outputs/p11_*.csv
docs/tmp/options-flow-analysis/outputs/p12_*.csv
docs/tmp/options-flow-analysis/outputs/p15_*.csv
docs/tmp/options-flow-analysis/outputs/p17_*.csv
docs/tmp/options-flow-analysis/outputs/p18_*.csv
docs/tmp/options-flow-analysis/outputs/p19_*.csv
docs/tmp/options-flow-analysis/outputs/p20_*.csv
docs/tmp/options-flow-analysis/outputs/p21_*.csv
docs/tmp/options-flow-analysis/outputs/p22_*.csv
docs/tmp/options-flow-analysis/outputs/v3_alerts.csv
docs/tmp/options-flow-analysis/outputs/*.log

# Lottery Finder — also ignore the 3 BIG computational intermediates
# (>20MB each, ~155MB combined). These are regenerable from committed
# scripts in <20 min total. Keep small load-bearing summaries tracked.
#   p14_event_triggers.csv  (60MB) — regen: p14_event_trigger.py (~10 min)
#   p27_policy_grid.csv     (22MB) — regen: p27_exit_policy_grid.py (~5 min)
#   macro_strike_gex.csv    (72MB) — regen: dump_macro_tables.ts (~1 min)
docs/tmp/options-flow-analysis/outputs/p14_event_triggers.csv
docs/tmp/options-flow-analysis/outputs/p27_policy_grid.csv
docs/tmp/options-flow-analysis/outputs/macro_strike_gex.csv

# Python bytecode cache in scripts dir
docs/tmp/options-flow-analysis/scripts/__pycache__/

# Periscope probe captures — contain authenticated UW page state.
# Generated by scripts/periscope-probe.mjs; user hands files back to
# Claude for selector identification. Do NOT commit.
docs/tmp/periscope-probe/

# Codex CLI tool config (per-clone, never committed)
.agents/
.codex/

# Scratch / one-off analyses default to untracked. Per
# CLAUDE.md, scratch files go in docs/tmp/ rather than /tmp or
# ~/Downloads. A blanket ignore keeps `git status` clean across
# active analysis sessions; the 147 already-tracked files in
# docs/tmp/ stay tracked (gitignore doesn't affect those).
# Add new files explicitly with `git add -f docs/tmp/<path>`
# when an analysis becomes worth keeping under version control.
docs/tmp/
.claude/codebase-map.json

# Local-only market data (Databento exports, ad-hoc CSVs). Keep
# raw bars out of git; large CSVs balloon the repo. The existing
# vix_family_daily.parquet stays tracked because gitignore does
# not affect already-committed files. Use `git add -f` to opt in.
data/*.csv
data/*.parquet

# Git worktrees (isolated workspaces for parallel sessions)
.worktrees/
