"""Per-setup evaluators. Each ``setup_N_*.py`` module exports an ``EVALUATOR``
instance implementing the ``harness.SetupEvaluator`` protocol.

Phase 1-8 will populate this directory one setup at a time.
"""
